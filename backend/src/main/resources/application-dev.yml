# AI Bookkeeping - Development Profile
# 本地开发环境配置，不会被提交到 Git

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_bookkeeping?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  ai:
    openai:
      api-key: sk-your-deepseek-api-key-here
      base-url: https://api.deepseek.com

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: myLocalDevSecretKeyForAIBookkeepingSystem2024MustBeLongEnough

glm:
  api-key: your-glm-api-key-here

logging:
  level:
    com.aibookkeeping: debug
